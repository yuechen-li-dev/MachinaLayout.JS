# Music Player Sample

This sample demonstrates a Claude-generated React music player UI wired through MachinaLayout views using the local root `machinalayout` package.

Designed and created by Claude 4.6 Sonnet one-shot, additional contributions by ChatGPT Codex 5.5.

## Local dependency

This sample depends on the repository root package via:

- `"machinalayout": "file:../.."`

## Run

```bash
# from repo root
npm run build

cd samples/music-player
npm install
npm run dev
```

## Build check

```bash
npm run build
```
