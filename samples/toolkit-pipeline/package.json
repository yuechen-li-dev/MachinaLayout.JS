{
  "name": "toolkit-pipeline-sample",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare:local-package": "node ../../tools/prepare-sample-subpath-imports.mjs .",
    "build": "npm run prepare:local-package && node --input-type=module -e \"import { rm } from 'node:fs/promises'; await rm('.generated', { recursive: true, force: true });\" && tsc -p tsconfig.json",
    "run": "node scripts/run-generated.mjs",
    "generate": "npm run build && npm run run"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
